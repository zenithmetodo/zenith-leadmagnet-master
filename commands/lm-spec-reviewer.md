---
description: "Code reviewer del SPEC del Lead Magnet"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-spec-reviewer`** (herramienta Agent, `subagent_type: lm-spec-reviewer`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-spec-reviewer`.
