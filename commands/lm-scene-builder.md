---
description: "Ingeniero de escenas SVG cinematicas"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-scene-builder`** (herramienta Agent, `subagent_type: lm-scene-builder`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-scene-builder`.
