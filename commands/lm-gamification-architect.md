---
description: "Disena COMO gamificar el lead magnet de forma epica: mecanicas (puntos, progreso, badges, streak, niveles, desbloqueos), recompensa variable,"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-gamification-architect`** (herramienta Agent, `subagent_type: lm-gamification-architect`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-gamification-architect`.
