---
description: "Especialista en jerarquia del color"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-color-hierarchy`** (herramienta Agent, `subagent_type: lm-color-hierarchy`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-color-hierarchy`.
