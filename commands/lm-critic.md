---
description: "Cliente ideal esceptico y auditor de Lead Magnets"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-critic`** (herramienta Agent, `subagent_type: lm-critic`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-critic`.
