---
description: "Experto en idea, angulo, dolor, promesa y naming de Lead Magnets"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-idea-naming`** (herramienta Agent, `subagent_type: lm-idea-naming`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-idea-naming`.
