---
description: "QA final integral del lead magnet: coherencia entre discovery, idea, copy, gamificacion, intro, front y back; checklist de calidad; verificacion de"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-qa-auditor`** (herramienta Agent, `subagent_type: lm-qa-auditor`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-qa-auditor`.
