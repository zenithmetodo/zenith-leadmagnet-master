---
description: "Maqueta la propuesta completa del lead magnet en un HTML estetico print-to-PDF para que el usuario la apruebe antes de construir el front"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-html-proposal`** (herramienta Agent, `subagent_type: lm-html-proposal`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-html-proposal`.
