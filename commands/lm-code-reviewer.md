---
description: "Revisa el codigo del front y back del lead magnet: funcionalidad, responsive, accesibilidad, rendimiento, ausencia de emojis (todo debe ser SVG) y"
argument-hint: "[lo que necesitas]"
---

Usa el subagente **`lm-code-reviewer`** (herramienta Agent, `subagent_type: lm-code-reviewer`) para esta tarea.

Peticion del usuario:
$ARGUMENTS

Si `$ARGUMENTS` viene vacio, pregunta brevemente el contexto necesario (nicho, avatar, oferta, nivel de consciencia, branding: colores/tipografia) antes de lanzar el subagente `lm-code-reviewer`.
